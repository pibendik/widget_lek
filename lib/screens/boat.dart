import 'package:flutter/material.dart';

import '../common/NavBar.dart';

class Boat extends StatelessWidget {
  const Boat({super.key});

  @override
  Widget build(BuildContext context) {
    return FilledScaffold(
        Container(
          child: Text("boat.dart"),
        )
    );
  }
}
